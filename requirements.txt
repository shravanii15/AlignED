# Dependencies for the data pipeline scripts (scraping, extraction, gap
# scoring, trends, recommendations) -- everything under scripts/.
# For the dashboard's own dependencies, see dashboard/requirements.txt
# (kept separate since Streamlit Cloud only needs the dashboard's, and
# doesn't need scikit-learn/sentence-transformers/kagglehub etc.)

requests>=2.31
beautifulsoup4>=4.12
python-dotenv>=1.0
kagglehub>=0.3
scipy>=1.11
scikit-learn>=1.3
sentence-transformers>=2.2
pandas>=2.0
numpy>=1.24

# Testing
pytest>=7.4
